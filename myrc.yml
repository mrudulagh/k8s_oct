apiVersion: v1
kind: ReplicationController
metadata:
  name: myjavaapp-rc
spec:
  replicas: 2
  selector:
    app: myjavaapp
    Version: 2.6.1
  template:
    metadata:
      name: myjavaapp
      labels:
        app: myjavaapp
        Version: 2.6.1
    spec:
      containers:
        - name: mycontainer
          image: maha4iac/myonlinebooking:1
          ports:
            - containerPort: 8080
